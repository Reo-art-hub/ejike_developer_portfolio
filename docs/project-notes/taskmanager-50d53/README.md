# Project notes (taskmanager-50d53)

Put Markdown files **here** (this folder or subfolders). They sync with Firestore `developerNotesFiles` on the project document when you run `sync-project-notes` / post-push pull / bidirectional notes sync.



Random `docs/**/*.md` paths **outside** this tree do **not** count as YuzuTask project notes for this project id.

See **docs/developers/PROJECT_LAYOUT_AND_BOOTSTRAP.md**.
